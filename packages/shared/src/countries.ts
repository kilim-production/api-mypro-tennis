export const countryCodes = [
  "AF",
  "AX",
  "AL",
  "DZ",
  "AS",
  "AD",
  "AO",
  "AI",
  "AQ",
  "AG",
  "AR",
  "AM",
  "AW",
  "AU",
  "AT",
  "AZ",
  "BS",
  "BH",
  "BD",
  "BB",
  "BY",
  "BE",
  "BZ",
  "BJ",
  "BM",
  "BT",
  "BO",
  "BQ",
  "BA",
  "BW",
  "BV",
  "BR",
  "IO",
  "BN",
  "BG",
  "BF",
  "BI",
  "CV",
  "KH",
  "CM",
  "CA",
  "KY",
  "CF",
  "TD",
  "CL",
  "CN",
  "CX",
  "CC",
  "CO",
  "KM",
  "CG",
  "CD",
  "CK",
  "CR",
  "CI",
  "HR",
  "CU",
  "CW",
  "CY",
  "CZ",
  "DK",
  "DJ",
  "DM",
  "DO",
  "EC",
  "EG",
  "SV",
  "GQ",
  "ER",
  "EE",
  "SZ",
  "ET",
  "FK",
  "FO",
  "FJ",
  "FI",
  "FR",
  "GF",
  "PF",
  "TF",
  "GA",
  "GM",
  "GE",
  "DE",
  "GH",
  "GI",
  "GR",
  "GL",
  "GD",
  "GP",
  "GU",
  "GT",
  "GG",
  "GN",
  "GW",
  "GY",
  "HT",
  "HM",
  "VA",
  "HN",
  "HK",
  "HU",
  "IS",
  "IN",
  "ID",
  "IR",
  "IQ",
  "IE",
  "IM",
  "IL",
  "IT",
  "JM",
  "JP",
  "JE",
  "JO",
  "KZ",
  "KE",
  "KI",
  "KP",
  "KR",
  "XK",
  "KW",
  "KG",
  "LA",
  "LV",
  "LB",
  "LS",
  "LR",
  "LY",
  "LI",
  "LT",
  "LU",
  "MO",
  "MG",
  "MW",
  "MY",
  "MV",
  "ML",
  "MT",
  "MH",
  "MQ",
  "MR",
  "MU",
  "YT",
  "MX",
  "FM",
  "MD",
  "MC",
  "MN",
  "ME",
  "MS",
  "MA",
  "MZ",
  "MM",
  "NA",
  "NR",
  "NP",
  "NL",
  "NC",
  "NZ",
  "NI",
  "NE",
  "NG",
  "NU",
  "NF",
  "MK",
  "MP",
  "NO",
  "OM",
  "PK",
  "PW",
  "PS",
  "PA",
  "PG",
  "PY",
  "PE",
  "PH",
  "PN",
  "PL",
  "PT",
  "PR",
  "QA",
  "RE",
  "RO",
  "RU",
  "RW",
  "BL",
  "SH",
  "KN",
  "LC",
  "MF",
  "PM",
  "VC",
  "WS",
  "SM",
  "ST",
  "SA",
  "SN",
  "RS",
  "SC",
  "SL",
  "SG",
  "SX",
  "SK",
  "SI",
  "SB",
  "SO",
  "ZA",
  "GS",
  "SS",
  "ES",
  "LK",
  "SD",
  "SR",
  "SJ",
  "SE",
  "CH",
  "SY",
  "TW",
  "TJ",
  "TZ",
  "TH",
  "TL",
  "TG",
  "TK",
  "TO",
  "TT",
  "TN",
  "TR",
  "TM",
  "TC",
  "TV",
  "UG",
  "UA",
  "AE",
  "GB",
  "US",
  "UM",
  "UY",
  "UZ",
  "VU",
  "VE",
  "VN",
  "VG",
  "VI",
  "WF",
  "EH",
  "YE",
  "ZM",
  "ZW"
] as const;

export type CountryCode = (typeof countryCodes)[number];

const countryCodeSet = new Set<string>(countryCodes);
const displayNames = new Intl.DisplayNames(["fr"], { type: "region" });

const manualCountryNames: Partial<Record<CountryCode, string>> = {
  XK: "Kosovo"
};

const legacyCountryAliases: Record<string, CountryCode> = {
  BEL: "BE",
  CAN: "CA",
  ESP: "ES",
  FRA: "FR",
  ITA: "IT",
  JPN: "JP",
  SUI: "CH",
  SWE: "SE",
  "BELGIQUE": "BE",
  "CANADA": "CA",
  "ESPAGNE": "ES",
  "FRANCE": "FR",
  "ITALIE": "IT",
  "JAPON": "JP",
  "SUEDE": "SE",
  "SUÈDE": "SE",
  "SUISSE": "CH",
  "ROYAUME UNI": "GB",
  "ROYAUME-UNI": "GB",
  "ETATS UNIS": "US",
  "ÉTATS UNIS": "US",
  "ETATS-UNIS": "US",
  "ÉTATS-UNIS": "US"
};

function normalizeText(value: string) {
  return value
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .replace(/[’'().]/g, "")
    .replace(/\s+/g, " ")
    .trim()
    .toUpperCase();
}

export function countryFlag(code: string) {
  const normalized = normalizeCountryCode(code);
  if (!normalized) return "🏳️";
  return [...normalized]
    .map((letter) => String.fromCodePoint(127397 + letter.charCodeAt(0)))
    .join("");
}

function rawCountryName(code: CountryCode) {
  return manualCountryNames[code] ?? displayNames.of(code) ?? code;
}

export function countryName(code: string) {
  const normalized = normalizeCountryCode(code);
  if (!normalized) return code;
  return rawCountryName(normalized);
}

export function countryLabel(code: string) {
  const normalized = normalizeCountryCode(code);
  if (!normalized) return code;
  return `${countryName(normalized)} ${countryFlag(normalized)}`;
}

export function normalizeCountryCode(value: string): CountryCode | null {
  const raw = value.trim();
  const upper = raw.toUpperCase();
  if (countryCodeSet.has(upper)) return upper as CountryCode;
  const alias = legacyCountryAliases[upper] ?? legacyCountryAliases[normalizeText(raw)];
  if (alias) return alias;
  const byName = countryCodes.find((code) => normalizeText(rawCountryName(code)) === normalizeText(raw));
  return byName ?? null;
}

export const countries = countryCodes
  .map((code) => ({
    code,
    name: rawCountryName(code),
    flag: countryFlag(code),
    label: countryLabel(code)
  }))
  .sort((left, right) => left.name.localeCompare(right.name, "fr"));
